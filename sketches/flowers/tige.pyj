# Text on path from Tom

WIDTH,HEIGHT = 500,500
size(WIDTH, HEIGHT)

fill(0,0,0)
rect(0, 0, WIDTH, HEIGHT)
mouse = canvas.mouse
shadow()
i = 170
def  setup(canvas):
    for i in range(25):
        path = new Path()
        path.moveto(i + WIDTH/4, HEIGHT/2)
        path.curveto(WIDTH/4, 170, WIDTH/2, 0, WIDTH-100, HEIGHT)  
        drawpath(path, {fill: null, stroke: [0,0.5,0,0.35]})
  
setup()
